- defaults:
    link:
      relink: true
      create: true

- clean: ["~", "~/.config"]

- link:
    ~/.config/git:
      path: git

    ~/.gitconfig:
      path: ~/.config/git/gitconfig

    ~/.config/shell:
      path: shell

    ~/.config/zsh:
      path: zsh

    ~/.zshrc:
      path: zsh/zshrc

    ~/.config/bash:
      path: bash

    ~/.zshrc:
      path: bash/bashrc

    ~/.config/tmux:
      path: tmux

    ~/.tmux.conf:
      path: tmux/tmux.conf
- shell:
    - [git submodule update --init --recursive, Installing submodules]
